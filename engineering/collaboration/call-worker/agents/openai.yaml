interface:
  display_name: "Call Worker"
  short_description: "Choose execution, model, effort, and request approval"
  default_prompt: "Use $call-worker to choose the execution mode, model, and reasoning effort, then request my approval for any dedicated task, worker, or code review."
